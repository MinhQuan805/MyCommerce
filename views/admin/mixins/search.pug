mixin search(filterStatus)
  form(id="form-search")
    .input-group 
      input(type="text" placeholder="Nhập từ khóa" name="keyword" class="form-keyword" value=keyword)
      button(class="btn btn-success" type="submit") Tìm
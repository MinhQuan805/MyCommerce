extends ../../layouts/auth.pug
include ../../mixins/alert.pug

block main
  +alert-error(5000)

  .container.my-5
    .row.justify-content-center
      .col-8
        h1(class="text-center") Đăng nhập
        form(
          action=`${prefixAdmin}/auth/login`
          method="POST"
        )
          div(class="form-group mt-2")
            label(for="email") Email
            input(
              type="email"
              class="form-control"
              id="email"
              name="email"
              required
            )

          div(class="form-group mt-2")
            label(for="password") Mật khẩu
            input(
              type="password"
              class="form-control"
              id="password"
              name="password"
              required
            )
          div(class="form-group mt-2")
            button(
              type="submit"
              class="btn btn-primary btn-block"
            ) Đăng nhập
        p(class="fs-3 mt-5") Tài khoản public
        p(class="fs-6") Email:  vominhquan@gmail.com
        p(class="fs-6") Mật khẩu:  abcd1234
